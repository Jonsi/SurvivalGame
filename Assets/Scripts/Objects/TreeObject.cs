using UnityEngine;

public class TreeObject : ChopableObject
{
    public TreeInstance treeData;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
